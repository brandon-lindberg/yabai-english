import { describe, test } from "vitest";

describe("timezone conversion", () => {
  test.todo("converts teacher availability to student timezone correctly");
  test.todo("handles DST boundaries for displayed lesson time");
});
