/**
 * OAuth return URL and post-connect redirects for Google integrations.
 * Keep in sync with the dashboard route that renders integration cards.
 */
export const DASHBOARD_GOOGLE_SETTINGS_PATH = "/dashboard/settings";
