# Test Fixtures

Use this folder for reusable factories and test data builders.

Recommended files:
- `user.factory.ts`
- `teacher.factory.ts`
- `booking.factory.ts`
- `payment.factory.ts`

Rules:
- Keep fixtures deterministic.
- Avoid hidden side effects.
- Prefer explicit defaults with per-test overrides.
