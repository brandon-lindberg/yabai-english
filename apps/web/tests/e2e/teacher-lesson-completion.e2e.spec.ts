import { test } from "@playwright/test";

test.describe("teacher lesson completion journey", () => {
  test("placeholder", async () => {
    test.fixme(
      true,
      "Implement TDD scenario: teacher views lesson -> joins -> completes -> adds notes",
    );
  });
});
