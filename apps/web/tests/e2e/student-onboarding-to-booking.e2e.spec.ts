import { test } from "@playwright/test";

test.describe("student onboarding to booking journey", () => {
  test("placeholder", async () => {
    test.fixme(
      true,
      "Implement TDD scenario: sign in -> onboarding -> teacher selection -> booking -> confirmation",
    );
  });
});
