import { test } from "@playwright/test";

test.describe("admin moderation journey", () => {
  test("placeholder", async () => {
    test.fixme(
      true,
      "Implement TDD scenario: admin edits user content and moderates report",
    );
  });
});
