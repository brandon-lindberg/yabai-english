import { describe, test } from "vitest";

describe("chat permissions", () => {
  test.todo("keeps student read-only before scheduled lesson exists");
  test.todo("allows two-way messaging only when teacher/admin enables it");
});
