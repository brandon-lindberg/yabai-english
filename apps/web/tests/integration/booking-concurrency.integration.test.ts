import { describe, test } from "vitest";

describe("booking concurrency", () => {
  test.todo("allows only one successful booking for the same slot");
  test.todo("returns user-facing conflict message for losing request");
});
